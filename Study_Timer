"""
Hello, this application is designed to time the users study session length and keep them motivated
Created by: DevSeanD
"""

import time
import random

x = int(input("How many minutes would you like to study for?"))
x = x * 60  # Convert seconds input to minutes
print("")
print("")
print("")


for y in range(1):  # generates one random number
    rand_num = (random.randint(1, 3))

# prints out a motivation quote dependand upon random generated number

if rand_num == 1:
    print("Never, never, never give up - Winston Churchill")

if rand_num == 2:
    print("Quality is not an act, it is a habit - Aristotle")

if rand_num == 3:
    print("It always seems impossible until its done - Nelson Mandela")


def procedure():

    time.sleep(x)


t0 = time.time()
procedure()
print("You have finished your study session of", int(x / 60), "minutes")
